{
    "version": 2,
    "env": {
        "APP_PORT": "8080"
    },
    "builds":[
        {
            "src": "*.go",
            "use": "@now/go"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "main.go"
        }
    ]
}